// include key is used for import library
// in the below code i import stdio.h which mean
// standard library for input output
#include <stdio.h>

// main is the key c program will execute
// it will always need `return 0` to indicate
// success return
int main(int argc, char *argv[]) {
  // C will always need semicolon `;` in the end
  // of line (it necesserrry)
  printf("Hello world!"); // printf are function came from library stdio.h
  return 0;
}
